[package]
name = "doom-rs"
version = "0.1.0"
edition = "2024"

[dependencies]
bool_ext = "0.7.0"
bitflags = "2.11.0"
rstest   = "0.26.1"
common = { path = "../common" }

[lib]
crate-type=["staticlib"]

[profile.release]
overflow-checks = false

[profile.dev]
overflow-checks = false